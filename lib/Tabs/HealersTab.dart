import 'package:flutter/material.dart';

class HealerView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("HealersPane"),
      ),
    );
  }
}
